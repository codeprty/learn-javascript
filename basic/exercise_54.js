// Math Properties

// Exercise 1: Display Math.PI
// Create a variable named piValue.
// Assign Math.PI to the variable.
// Log the value to the console.
const piValue = Math.PI;
console.log(piValue);

// Exercise 2: Store Math.E in a Variable
// Create a variable named eValue.
// Assign Math.E to the variable.
// Log the value.
const eValue = Math.E;
console.log(eValue);

// Exercise 3: Display Square Root of 2
// Create a variable named sqrtTwo.
// Assign Math.SQRT2 to the variable.
// Log the value.
const sqrtTwo = Math.SQRT2;
console.log(sqrtTwo);

// Exercise 4: Display Square Root of 1/2
// Create a variable named sqrtHalf.
// Assign Math.SQRT1_2 to the variable.
// Log the value.
const sqrtHalf = Math.SQRT1_2;
console.log(sqrtHalf);

// Exercise 5: Create a Message Using Math.PI
// Create a string that says: "The value of PI is <value>"
// Use Math.PI to insert the value.
// Log the string.
const piString = `The value of PI is ${Math.PI}`;
console.log(piString);

// Exercise 6: Create a Message Using Math.E
// Create a string that says: "The value of E is <value>"
// Use Math.E in the string.
// Log the result.
const eString = `The value of E is ${Math.E}`;
console.log(eString);

// Exercise 7: Compare Math.PI and Math.E
// Create two variables and assign Math.PI and Math.E.
// Log both values to see which one is larger.
const a = Math.PI;
const b = Math.E;
console.log(a);
console.log(b);

// Exercise 8: Assign All Math Properties
// Create four variables for Math.PI, Math.E, Math.SQRT2, and Math.SQRT1_2.
// Log all four variables.
const numA = Math.PI;
const numB = Math.E;
const numC = Math.SQRT2;
const numD = Math.SQRT1_2;
console.log(numA);
console.log(numB);
console.log(numC);
console.log(numD);

// Exercise 9: Simple Sentence with Math.SQRT2
// Create a string that says: "The square root of 2 is <value>"
// Use Math.SQRT2 in the string.
// Log the string.
const sqrt2String = `The square root of 2 is ${Math.SQRT2}`;
console.log(sqrt2String);

// Exercise 10: Simple Sentence with Math.SQRT1_2
// Create a string that says: "The square root of 1/2 is <value>"
// Use Math.SQRT1_2 in the string.
// Log the string.
const sqrt1String = `The square root of 1/2 is ${Math.SQRT1_2}`;
console.log(sqrt1String);