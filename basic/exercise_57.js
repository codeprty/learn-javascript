// Math Methods: Calculations & Functions

// Exercise 1: Math.pow() Basic
// Create a variable named base with the value 2.
// Create a variable named exponent with the value 3.
// Use Math.pow() to calculate the power.
// Log the result.
const base = 2;
const exponent = 3;
console.log(Math.pow(base, exponent));

// Exercise 2: Math.pow() with Different Values
// Create two variables with values 5 and 2.
// Use Math.pow() to calculate the result.
// Log the value.
const a = 5;
const b = 2;
console.log(Math.pow(a, b));

// Exercise 3: Math.sqrt() Basic
// Create a variable named number with the value 16.
// Use Math.sqrt() to find the square root.
// Log the result.
const number = 16;
console.log(Math.sqrt(number));

// Exercise 4: Math.sqrt() with Another Number
// Create a variable named value with the value 25.
// Use Math.sqrt() on the value.
// Log the result.
const value = 25;
console.log(Math.sqrt(value));

// Exercise 5: Math.random() Basic
// Use Math.random() to generate a random number.
// Store it in a variable.
// Log the value.
const randomNum = Math.random();
console.log(randomNum);

// Exercise 6: Math.random() Multiple Times
// Create two variables using Math.random().
// Log both values.
const random1 = Math.random();
const random2 = Math.random();
console.log(random1);
console.log(random2);

// Exercise 7: Math.sin() Basic
// Create a variable named x with the value 0.
// Use Math.sin() on the x.
// Log the result.
const x = 0;
console.log(Math.sin(x));

// Exercise 8: Math.cos() Basic
// Create a variable named y with the value 0.
// Use Math.cos() on the y.
// Log the result.
const y = 0;
console.log(Math.cos(y));

// Exercise 9: Math.tan() Basic
// Create a variable named z with the value 0.
// Use Math.tan() on the z.
// Log the result.
const z = 0;
console.log(Math.tan(z));

// Exercise 10: Mixed Math Calculations
// Create one variable with the value 9.
// Use Math.sqrt() and Math.pow() at least once.
// Log all results.
const n = 9;
console.log(Math.sqrt(n));
console.log(Math.pow(n, 2));