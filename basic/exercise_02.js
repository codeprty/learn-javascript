// Arithmetic Operators

// Exercise 1: Addition
// Create two variables: num1 and num2.
// Assign them any numbers.
// Create a variable sum that stores the result of num1 + num2.
// Log sum to the console.
let num1 = 3;
let num2 = 5;
let sum = num1 + num2;
console.log(sum);

// Exercise 2: Subtraction
// Create two variables: num1 and num2.
// Assign them any numbers.
// Create a variable difference that stores num1 - num2.
// Log difference to the console.
num1 = 10;
num2 = 7;
let difference = num1 - num2;
console.log(difference);

// Exercise 3: Multiplication
// Create two variables: num1 and num2.
// Assign them any numbers.
// Create a variable product that stores num1 * num2.
// Log product to the console.
num1 = 5;
num2 = 3;
let product = num1 * num2;
console.log(product);

// Exercise 4: Division
// Create two variables: num1 and num2.
// Assign them any numbers (num2 should not be zero).
// Create a variable result that stores num1 / num2.
// Log result to the console.
num1 = 8;
num2 = 2;
let result = num1 / num2;
console.log(result);

// Exercise 5: Modulus (Remainder)
// Create two variables: num1 and num2.
// Assign them any numbers.
// Create a variable remainder that stores num1 % num2.
// Log remainder to the console.
num1 = 7;
num2 = 2;
let remainder = num1 % num2;
console.log(remainder);

// Exercise 6: Exponentiation
// Create two variables: base and exponent.
// Assign any numbers.
// Create a variable power that stores base ** exponent.
// Log power to the console.
let base = 3;
let exponent = 2;
let power = base ** exponent;
console.log(power);

// Exercise 7: Increment
// Create a variable counter and set it to 5.
// Increase counter by 1 using increment operator (++).
// Log the final value of counter.
let counter = 5;
counter++;
console.log(counter);

// Exercise 8: Decrement
// Create a variable counter and set it to 10.
// Decrease counter by 1 using decrement operator (--).
// Log the final value of counter.
counter = 10;
counter--;
console.log(counter);

// Exercise 9: Operator Precedence
// Create three variables: x = 5, y = 10, z = 2.
// Create a variable result that stores x + y * z.
// Log result to the console.
// (Notice how multiplication happens before addition.)
let x = 5;
let y = 10;
let z = 2;
result = x + y * z;
console.log(result);

// Exercise 10: Mixed Operators
// Create four variables: a = 8, b = 4, c = 2, d = 3.
// Create a variable result that stores (a + b) * c - d.
// Log result to the console.
let a = 8;
let b = 4;
let c = 2;
let d = 3;
result = (a + b) * c - d;
console.log(result);