// Math Methods: Rounding & Truncation

// Exercise 1: Math.round() Basic
// Create a variable named num with the value 4.6.
// Use Math.round() to round the number.
// Log the result.
const num = 4.6;
console.log(Math.round(num));

// Exercise 2: Math.round() with Decimal
// Create a variable named score with the value 7.3.
// Round the score using Math.round().
// Log the rounded value.
const score = 7.3;
console.log(Math.round(score));

// Exercise 3: Math.ceil() Basic
// Create a variable named price with the value 10.1.
// Use Math.ceil() to round the number up.
// Log the result.
const price = 10.1;
console.log(Math.ceil(price));

// Exercise 4: Math.ceil() with Decimal
// Create a variable named distance with the value 5.8.
// Use Math.ceil() on the distance.
// Log the result.
const distance = 5.8;
console.log(Math.ceil(distance));

// Exercise 5: Math.floor() Basic
// Create a variable named temperature with the value 23.9.
// Use Math.floor() to round the number down.
// Log the result.
const temperature = 23.9;
console.log(Math.floor(temperature));

// Exercise 6: Math.floor() with Decimal
// Create a variable named rating with the value 4.7.
// Use Math.floor() on the rating.
// Log the result.
const rating = 4.7;
console.log(Math.floor(rating));

// Exercise 7: Math.trunc() Basic
// Create a variable named amount with the value 9.8.
// Use Math.trunc() to remove the decimal part.
// Log the result.
const amount = 9.8;
console.log(Math.trunc(amount));

// Exercise 8: Math.trunc() with Decimal
// Create a variable named value with the value 15.2.
// Use Math.trunc() on the value.
// Log the result.
const value = 15.2;
console.log(Math.trunc(value));

// Exercise 9: Compare Rounding Methods
// Create a variable named number with the value 6.5.
// Apply Math.round(), Math.ceil(), and Math.floor() to the number.
// Log each result.
const number = 6.5;
console.log(Math.round(number));
console.log(Math.ceil(number));
console.log(Math.floor(number));

// Exercise 10: Mixed Rounding Practice
// Create four variables with the values: 2.3, 2.7, 8.1, 8.9.
// Use Math.round(), Math.ceil(), Math.floor(), and Math.trunc() once each.
// Log all results.
const a = 2.3;
const b = 2.7;
const c = 8.1;
const d = 8.9;
console.log(Math.round(a));
console.log(Math.ceil(b));
console.log(Math.floor(c));
console.log(Math.trunc(d));